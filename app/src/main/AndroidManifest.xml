<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.applicationkeuanganpribadi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListRecordActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".AddRecActivity2" />
        <activity android:name=".AddRecActivity" />
        <activity android:name=".SuccessActivity" />
        <activity android:name=".ContohActivity" />
        <activity android:name=".PinnActivity" />
        <activity android:name=".AboutActivity2" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".CurrActivity" />
        <activity android:name=".PrivasiActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>